# 📧 Руководство по Email-аутентификации

## Настроенные функции

### ✅ 1. Подтверждение регистрации через email
При регистрации пользователь получает письмо со ссылкой для подтверждения email.

### ✅ 2. Восстановление пароля
Пользователь может восстановить пароль через email.

---

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
# Gmail конфигурация
NEXT_PUBLIC_NODEMAILER_USER=molokoedovmp@gmail.com
NEXT_PUBLIC_NODEMAILER_PASSWORD=wbrijluzkyyevygh

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="ваш-секретный-ключ"

# PostgreSQL
DATABASE_URL="postgresql://..."
```

---

## 📝 Как это работает

### Регистрация с подтверждением email

1. Пользователь заполняет форму регистрации
2. Система создает аккаунт в базе данных
3. Генерируется токен верификации (действителен 24 часа)
4. На email отправляется письмо со ссылкой подтверждения
5. Пользователь переходит по ссылке `/verify-email?token=...`
6. Email подтверждается, токен удаляется
7. Пользователь перенаправляется на страницу входа

### Восстановление пароля

1. Пользователь нажимает "Забыли пароль?" на странице входа
2. Вводит свой email на странице `/forgot-password`
3. Система генерирует токен (действителен 1 час)
4. На email отправляется письмо со ссылкой восстановления
5. Пользователь переходит по ссылке `/reset-password?token=...`
6. Вводит новый пароль
7. Пароль обновляется, токен удаляется
8. Пользователь перенаправляется на страницу входа

---

## 🗂️ Структура файлов

### Библиотеки
- `lib/email.ts` - конфигурация nodemailer и функции отправки email

### API Routes
- `app/api/auth/register/route.ts` - регистрация + отправка email
- `app/api/auth/verify-email/route.ts` - подтверждение email
- `app/api/auth/forgot-password/route.ts` - запрос восстановления пароля
- `app/api/auth/reset-password/route.ts` - сброс пароля

### Страницы
- `app/register/user/page.tsx` - форма регистрации
- `app/verify-email/page.tsx` - страница подтверждения email
- `app/forgot-password/page.tsx` - форма запроса восстановления
- `app/reset-password/page.tsx` - форма установки нового пароля

### Контекст
- `contexts/AuthContext.tsx` - добавлена функция `signUp()`

---

## 📧 Шаблоны писем

### Письмо подтверждения регистрации
- **Тема:** Подтверждение регистрации - ПТСР Эксперт
- **Содержимое:** Приветствие + кнопка "Подтвердить email" + ссылка
- **Срок действия:** 24 часа

### Письмо восстановления пароля
- **Тема:** Восстановление пароля - ПТСР Эксперт
- **Содержимое:** Инструкции + кнопка "Сбросить пароль" + ссылка
- **Срок действия:** 1 час

---

## 🔐 Безопасность

### Токены
- Генерируются криптографически безопасным способом (crypto.randomBytes)
- Хранятся в таблице `verification_tokens`
- Имеют срок действия
- Удаляются после использования

### Пароли
- Хешируются с помощью bcryptjs (12 раундов)
- Минимальная длина: 6 символов
- Никогда не передаются в открытом виде

### Email
- Отправляется через Gmail SMTP
- Использует пароль приложения (app password)
- Все ссылки содержат уникальные токены

---

## 🧪 Тестирование

### Тест регистрации
1. Перейдите на `/register/user`
2. Заполните форму
3. Проверьте email (также папку спам)
4. Перейдите по ссылке подтверждения
5. Войдите в систему

### Тест восстановления пароля
1. Перейдите на `/forgot-password`
2. Введите email
3. Проверьте почту
4. Перейдите по ссылке
5. Установите новый пароль
6. Войдите с новым паролем

---

## ❓ Возможные проблемы

### Письма не приходят
1. Проверьте переменные окружения
2. Убедитесь, что пароль приложения Gmail правильный
3. Проверьте папку "Спам"
4. Проверьте логи сервера на ошибки

### Ошибка "Токен истек"
- Токен действителен ограниченное время
- Запросите новое письмо

### Ошибка "Неверный токен"
- Токен мог быть уже использован
- Запросите новое письмо

---

## 🎨 Кастомизация

### Изменение шаблонов писем
Отредактируйте файл `lib/email.ts`:
- Измените HTML в функциях `sendVerificationEmail` и `sendPasswordResetEmail`
- Обновите стили, текст, структуру

### Изменение срока действия токенов
В API routes измените время:
```typescript
// Для регистрации (24 часа)
const expires = new Date(Date.now() + 24 * 3600000)

// Для восстановления пароля (1 час)
const expires = new Date(Date.now() + 3600000)
```

### Использование другого email провайдера
В `lib/email.ts` измените конфигурацию transporter:
```typescript
export const transporter = nodemailer.createTransport({
  host: "smtp.example.com",
  port: 587,
  secure: false,
  auth: {
    user: process.env.EMAIL_USER,
    pass: process.env.EMAIL_PASSWORD,
  },
})
```

---

## ✅ Готово!

Теперь у вас есть полноценная система:
- ✅ Регистрация с подтверждением email
- ✅ Восстановление пароля через email
- ✅ Безопасное хранение токенов
- ✅ Красивые письма с брендингом

**Важно:** Для production убедитесь, что:
- Используете надежный SMTP сервис
- Настроены правильные DNS записи (SPF, DKIM, DMARC)
- NEXTAUTH_URL указывает на ваш production домен

