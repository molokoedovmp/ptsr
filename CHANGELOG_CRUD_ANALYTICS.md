# Changelog - CRUD и Аналитика

## ✅ Выполненные задачи

### 🗂️ CRUD для управления статьями

#### Созданные файлы:
1. **`/app/admin/articles/page.tsx`** - Страница списка всех статей
   - Отображение всех статей в таблице
   - Быстрая публикация/снятие с публикации
   - Удаление статей с подтверждением
   - Счетчик просмотров
   - Ссылки на редактирование

2. **`/app/admin/articles/new/page.tsx`** - Страница создания статьи
   - Форма с валидацией
   - Автогенерация slug из заголовка (с транслитерацией)
   - Поддержка категорий и тегов
   - Опция публикации
   - Загрузка обложки

3. **`/app/admin/articles/[id]/edit/page.tsx`** - Страница редактирования
   - Загрузка существующих данных
   - Полное редактирование всех полей
   - Сохранение изменений

4. **`/app/api/admin/articles/route.ts`** - API для списка и создания
   - `GET` - Получение всех статей
   - `POST` - Создание новой статьи

5. **`/app/api/admin/articles/[id]/route.ts`** - API для конкретной статьи
   - `GET` - Получение статьи по ID
   - `PATCH` - Обновление статьи
   - `DELETE` - Удаление статьи

### 📊 Полная аналитика посещений

#### Созданные файлы:

1. **`/components/AnalyticsTracker.tsx`** - Компонент автоматического отслеживания
   - Отслеживание всех переходов между страницами
   - Определение времени на странице
   - Получение геолокации через ipapi.co
   - Определение устройства, ОС, браузера
   - Работа с sessionStorage для уникальных сессий

2. **`/app/api/analytics/track/route.ts`** - API для записи аналитики
   - Сохранение данных о посещениях
   - Автоматическое определение устройства из User Agent
   - Определение ОС и браузера
   - Запись IP адреса и геолокации

3. **`/app/api/admin/analytics/visitors/route.ts`** - API для получения аналитики
   - Агрегация по странам (топ-10)
   - Агрегация по страницам (топ-10)
   - Агрегация по устройствам
   - Агрегация по ОС
   - Агрегация по браузерам
   - Посещения по дням
   - Уникальные посетители
   - Среднее время на сайте
   - Фильтрация по периодам (7, 14, 30, 90 дней)

4. **`/app/api/admin/analytics/route.ts`** - API для общей аналитики
   - Общая статистика пользователей
   - Статистика курсов, статей, психологов
   - Рост пользователей по дням
   - Топ статей по просмотрам
   - Популярность курсов

5. **`/app/admin/analytics/page.tsx`** - Обновленная страница аналитики
   - Общие метрики (пользователи, курсы, статьи)
   - **Новая секция**: Аналитика посещений
     - Всего посещений
     - Уникальных посетителей
     - Среднее время на сайте
     - График посещений по дням
     - Топ-10 стран с визуализацией
     - Топ-10 страниц
     - Распределение по устройствам (с прогресс-барами)
     - Распределение по ОС (с прогресс-барами)
     - Распределение по браузерам (с прогресс-барами)
   - Селектор периода (7/14/30/90 дней)
   - Красивая визуализация данных

### 🗄️ База данных

#### Обновления схемы Prisma:

```prisma
model VisitorAnalytics {
  id            String   @id @default(cuid())
  sessionId     String   @map("session_id")
  userId        String?  @map("user_id")
  pagePath      String   @map("page_path")
  pageTitle     String?  @map("page_title")
  referrer      String?
  
  // Геолокация
  country       String?
  countryCode   String?  @map("country_code")
  city          String?
  region        String?
  
  // Устройство и браузер
  device        String?
  os            String?
  browser       String?
  screenWidth   Int?     @map("screen_width")
  screenHeight  Int?     @map("screen_height")
  
  // Язык и временная зона
  language      String?
  timezone      String?
  
  // IP и User Agent
  ipAddress     String?  @map("ip_address")
  userAgent     String?  @map("user_agent") @db.Text
  
  // Временные метрики
  timestamp     DateTime @default(now())
  duration      Int?
  
  @@index([userId, timestamp])
  @@index([pagePath, timestamp])
  @@index([country, timestamp])
  @@index([sessionId])
  @@map("visitor_analytics")
}
```

### 🎨 Интеграция в систему

1. **`/app/layout.tsx`** - Добавлен `AnalyticsTracker`
   - Автоматическое отслеживание на всех страницах
   - Работает для авторизованных и анонимных пользователей

2. **`/app/admin/dashboard/page.tsx`** - Уже имеет ссылки:
   - "Управление статьями" → `/admin/articles`
   - "Аналитика" → `/admin/analytics`

## 📊 Отслеживаемые данные

### Геолокация
- ✅ Страна
- ✅ Код страны
- ✅ Город
- ✅ Регион

### Устройства и технологии
- ✅ Тип устройства (Desktop, Mobile, Tablet)
- ✅ Операционная система (Windows, MacOS, Linux, Android, iOS)
- ✅ Браузер (Chrome, Firefox, Safari, Edge, Opera)
- ✅ Разрешение экрана

### Поведение пользователей
- ✅ Посещенные страницы
- ✅ Время на каждой странице
- ✅ Реферер (откуда пришли)
- ✅ Язык браузера
- ✅ Временная зона

### Технические данные
- ✅ IP адрес
- ✅ User Agent
- ✅ Уникальный ID сессии

## 🚀 Как использовать

### 1. База данных уже обновлена!
```bash
✅ Выполнено: npx prisma db push
```

### 2. Перезапустите сервер
```bash
npm run dev
```

### 3. Откройте админ-панель
```
http://localhost:3000/admin/dashboard
```

### 4. Доступные разделы:

#### Управление статьями:
- Список статей: `/admin/articles`
- Создать статью: `/admin/articles/new`
- Редактировать: `/admin/articles/[id]/edit`

#### Аналитика:
- Общая аналитика: `/admin/analytics`
- Выберите период (7, 14, 30, 90 дней)
- Просматривайте детальную статистику

## 📈 Возможности аналитики

### Видимые метрики:
1. **Общие показатели**:
   - Всего посещений за период
   - Уникальных посетителей
   - Среднее время на сайте (в секундах)

2. **Временная динамика**:
   - График посещений по дням
   - Сравнение активности

3. **Географические данные**:
   - Топ-10 стран по посещениям
   - Визуализация распределения

4. **Популярные страницы**:
   - Топ-10 самых посещаемых страниц
   - Количество просмотров каждой

5. **Технические показатели**:
   - Распределение по устройствам (Desktop/Mobile/Tablet)
   - Распределение по ОС
   - Распределение по браузерам
   - Прогресс-бары для наглядности

## ⚙️ Настройка геолокации

По умолчанию используется **ipapi.co** (бесплатный сервис, 30,000 запросов/месяц).

### Альтернативы для production:

**MaxMind GeoIP2** (платный, более точный):
```typescript
// В компоненте AnalyticsTracker.tsx
const response = await fetch('https://geoip.maxmind.com/geoip/v2.1/city/me', {
  headers: {
    'Authorization': `Basic ${btoa('accountId:licenseKey')}`
  }
})
```

**IP-API** (бесплатный, но с ограничениями):
```typescript
const response = await fetch('http://ip-api.com/json/')
```

## 🔒 Безопасность и приватность

- ✅ Только администраторы имеют доступ к аналитике
- ✅ IP адреса хранятся для анализа (можно анонимизировать)
- ✅ Данные защищены ролевой системой
- ⚠️ Для GDPR compliance рассмотрите анонимизацию IP

### Анонимизация IP (для GDPR):
```typescript
// В /app/api/analytics/track/route.ts
const anonymizeIP = (ip: string) => {
  const parts = ip.split('.')
  parts[parts.length - 1] = '0'
  return parts.join('.')
}

ipAddress: anonymizeIP(ip)
```

## 🐛 Возможные проблемы

### Проблема: Аналитика не записывается
**Решение**: 
- Проверьте консоль браузера на ошибки
- Убедитесь, что таблица `visitor_analytics` создана
- Проверьте, что `AnalyticsTracker` добавлен в `layout.tsx`

### Проблема: Геолокация не определяется
**Решение**:
- Проверьте доступность ipapi.co
- Рассмотрите использование альтернативного сервиса
- Геолокация не критична, система работает и без нее

### Проблема: Медленная загрузка аналитики
**Решение**:
- Убедитесь, что все индексы созданы (они есть в схеме)
- Уменьшите период анализа
- Добавьте кэширование на уровне API

## 📝 Дополнительные файлы

- **`CRUD_AND_ANALYTICS_GUIDE.md`** - Подробное руководство
- **`CHANGELOG_CRUD_ANALYTICS.md`** - Этот файл (список изменений)

## 🎯 Следующие шаги (опционально)

### Дополнительные CRUD:
- [ ] Управление курсами (Create, Read, Update, Delete)
- [ ] Управление видео (Create, Read, Update, Delete)
- [ ] Управление пользователями (просмотр, блокировка, удаление)
- [ ] Массовые операции (bulk edit/delete)

### Улучшения аналитики:
- [ ] Экспорт в CSV/Excel
- [ ] Email-отчеты
- [ ] Настраиваемые дашборды
- [ ] Тепловые карты
- [ ] Воронки конверсии
- [ ] A/B тестирование

---

## ✨ Итого добавлено:

### Файлы (всего: 9 новых + 3 обновленных):

**Новые:**
1. `/app/admin/articles/page.tsx`
2. `/app/admin/articles/new/page.tsx`
3. `/app/admin/articles/[id]/edit/page.tsx`
4. `/app/api/admin/articles/route.ts`
5. `/app/api/admin/articles/[id]/route.ts`
6. `/app/api/analytics/track/route.ts`
7. `/app/api/admin/analytics/visitors/route.ts`
8. `/components/AnalyticsTracker.tsx`
9. `/app/api/admin/analytics/route.ts`

**Обновленные:**
1. `/app/admin/analytics/page.tsx` - Добавлена секция с аналитикой посещений
2. `/app/layout.tsx` - Добавлен AnalyticsTracker
3. `/prisma/schema.prisma` - Добавлена модель VisitorAnalytics

### Функционал:
- ✅ Полный CRUD для статей
- ✅ Автоматическое отслеживание посещений
- ✅ Детальная аналитика по странам, устройствам, ОС, браузерам
- ✅ Аналитика популярных страниц
- ✅ Временная аналитика
- ✅ Защищенный доступ только для администраторов

---

**Версия**: 1.0.0  
**Дата**: 13 ноября 2025  
**Статус**: ✅ Готово к использованию

