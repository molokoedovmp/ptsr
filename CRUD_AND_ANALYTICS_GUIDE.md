# Руководство по CRUD и Аналитике

## 📋 Обзор

В систему добавлены полноценные CRUD операции для управления контентом и детальная аналитика посещений с отслеживанием стран, устройств, браузеров и страниц.

## 🎯 Добавленные функции

### 1. CRUD для статей

#### Страницы:
- `/admin/articles` - Список всех статей с возможностью фильтрации
- `/admin/articles/new` - Создание новой статьи
- `/admin/articles/[id]/edit` - Редактирование статьи

#### Функционал:
- ✅ Создание статьи (Create)
- ✅ Просмотр списка статей (Read)
- ✅ Редактирование статьи (Update)
- ✅ Удаление статьи (Delete)
- ✅ Публикация/снятие с публикации
- ✅ Категории и теги
- ✅ Счетчик просмотров
- ✅ Автоматическая генерация slug из заголовка

#### API Endpoints:
```
GET    /api/admin/articles          - Получить список статей
POST   /api/admin/articles          - Создать статью
GET    /api/admin/articles/[id]     - Получить статью по ID
PATCH  /api/admin/articles/[id]     - Обновить статью
DELETE /api/admin/articles/[id]     - Удалить статью
```

### 2. Аналитика посещений

#### Отслеживаемые данные:
- 🌍 **Геолокация**: Страна, код страны, город, регион
- 💻 **Устройства**: Desktop, Mobile, Tablet
- 🖥️ **Операционные системы**: Windows, MacOS, Linux, Android, iOS
- 🌐 **Браузеры**: Chrome, Firefox, Safari, Edge, Opera
- 📱 **Разрешение экрана**: Ширина и высота
- 🌏 **Язык и временная зона**
- 📄 **Посещенные страницы**
- ⏱️ **Время на странице**
- 🔗 **Реферер**
- 🆔 **IP адрес и User Agent**

#### Аналитическая панель:
- Общие метрики (посещения, уникальные посетители, среднее время)
- График посещений по дням
- Топ-10 стран
- Топ-10 страниц
- Распределение по устройствам
- Распределение по ОС
- Распределение по браузерам
- Фильтр по периодам (7, 14, 30, 90 дней)

### 3. Автоматическое отслеживание

Компонент `AnalyticsTracker` автоматически:
- Отслеживает все переходы между страницами
- Записывает время пребывания на странице
- Определяет геолокацию пользователя
- Собирает информацию об устройстве
- Работает для авторизованных и анонимных пользователей

## 🚀 Установка и настройка

### 1. Применить миграцию базы данных

```bash
cd /Users/mikhailmolokoedov/Documents/ptsr
npx prisma migrate dev --name add_visitor_analytics
```

Эта команда:
- Создаст таблицу `visitor_analytics` в базе данных
- Добавит все необходимые индексы для быстрых запросов
- Обновит Prisma Client

### 2. Перезапустить сервер разработки

```bash
npm run dev
```

## 📊 Использование

### Для администраторов:

1. **Управление статьями**:
   - Откройте `/admin/dashboard`
   - Перейдите в "Управление статьями"
   - Создавайте, редактируйте и удаляйте статьи

2. **Просмотр аналитики**:
   - Откройте `/admin/dashboard`
   - Перейдите в "Аналитика"
   - Выберите период для анализа
   - Просматривайте детальную статистику

3. **Экспорт данных** (будущая функция):
   - Возможность экспорта в CSV/Excel
   - Детальные отчеты

## 🗄️ Структура базы данных

### Таблица: visitor_analytics

```sql
CREATE TABLE visitor_analytics (
  id            TEXT PRIMARY KEY,
  session_id    TEXT NOT NULL,
  user_id       TEXT,
  page_path     TEXT NOT NULL,
  page_title    TEXT,
  referrer      TEXT,
  
  -- Геолокация
  country       TEXT,
  country_code  TEXT,
  city          TEXT,
  region        TEXT,
  
  -- Устройство
  device        TEXT,
  os            TEXT,
  browser       TEXT,
  screen_width  INTEGER,
  screen_height INTEGER,
  
  -- Язык и временная зона
  language      TEXT,
  timezone      TEXT,
  
  -- Технические данные
  ip_address    TEXT,
  user_agent    TEXT,
  
  -- Временные метрики
  timestamp     TIMESTAMP DEFAULT NOW(),
  duration      INTEGER
);

-- Индексы для быстрых запросов
CREATE INDEX idx_user_timestamp ON visitor_analytics(user_id, timestamp);
CREATE INDEX idx_page_timestamp ON visitor_analytics(page_path, timestamp);
CREATE INDEX idx_country_timestamp ON visitor_analytics(country, timestamp);
CREATE INDEX idx_session ON visitor_analytics(session_id);
```

## 🔧 Настройка геолокации

По умолчанию используется бесплатный сервис ipapi.co для определения геолокации.

### Альтернативные сервисы:

1. **MaxMind GeoIP2** (рекомендуется для production):
```javascript
// В AnalyticsTracker.tsx
const response = await fetch(`https://geoip.maxmind.com/geoip/v2.1/city/me`, {
  headers: { 'Authorization': `Basic ${btoa('accountId:licenseKey')}` }
})
```

2. **IP-API**:
```javascript
const response = await fetch('http://ip-api.com/json/')
```

## 📈 Будущие улучшения

- [ ] Экспорт отчетов в CSV/Excel
- [ ] Настраиваемые дашборды
- [ ] Email-уведомления о важных событиях
- [ ] A/B тестирование
- [ ] Тепловые карты кликов
- [ ] Воронки конверсии
- [ ] Интеграция с Google Analytics
- [ ] CRUD для курсов, видео, пользователей
- [ ] Массовые операции (bulk operations)

## 🔒 Безопасность

- ✅ Все данные защищены ролевой системой доступа
- ✅ Только администраторы могут просматривать аналитику
- ✅ IP-адреса могут быть анонимизированы для GDPR
- ✅ Возможность отключения отслеживания для определенных ролей

## 🐛 Решение проблем

### Аналитика не отслеживается

1. Проверьте, что компонент `AnalyticsTracker` добавлен в `layout.tsx`
2. Убедитесь, что таблица `visitor_analytics` создана в БД
3. Проверьте консоль браузера на ошибки

### Геолокация не определяется

1. Проверьте доступность API ipapi.co
2. Рассмотрите использование альтернативного сервиса
3. Можно отключить геолокацию, если она не критична

### Медленные запросы к аналитике

1. Убедитесь, что все индексы созданы
2. Увеличьте период кэширования
3. Рассмотрите агрегацию данных для старых периодов

## 📝 Примеры использования API

### Получить аналитику за 30 дней

```javascript
const response = await fetch('/api/admin/analytics/visitors?period=30')
const data = await response.json()

console.log('Посещений:', data.totalVisits)
console.log('Уникальных:', data.uniqueVisitors)
console.log('Среднее время:', data.avgDuration)
```

### Создать новую статью

```javascript
const response = await fetch('/api/admin/articles', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    title: 'Новая статья',
    slug: 'novaya-statya',
    content: 'Содержание статьи...',
    excerpt: 'Краткое описание',
    category: 'NEWS',
    tags: ['тег1', 'тег2'],
    published: true
  })
})
```

### Обновить статью

```javascript
const response = await fetch(`/api/admin/articles/${articleId}`, {
  method: 'PATCH',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    published: true
  })
})
```

## 🎨 Кастомизация

### Изменить период по умолчанию

В `/app/admin/analytics/page.tsx`:
```typescript
const [period, setPeriod] = useState('30') // Вместо '7'
```

### Добавить новые метрики

1. Обновите схему Prisma
2. Создайте миграцию
3. Обновите API endpoint
4. Обновите интерфейс аналитики

## 📞 Поддержка

При возникновении проблем или вопросов:
1. Проверьте логи сервера (`npm run dev`)
2. Проверьте консоль браузера
3. Убедитесь, что все миграции применены
4. Проверьте переменные окружения в `.env`

---

**Разработано для ПТСР Эксперт**  
Версия: 1.0.0  
Дата: 13 ноября 2025

